#include<stdio.h>
int main()
{
    int n,oN,rem,r=0;
    scanf("%d",&n);
    oN= number;
    while (oN!=0)
    {
        rem= oN%10;
        r+= rem*remr*rem;
        oN/= 10;
    }
    if(r==n)
        printf("%d is an Armstrong number.",n);
    else
        printf("%d is not an Armstrong number.",n);

    return 0;
}
